# ASK GPT - Hongye Quote Generator

## 时间
2026-06-07 14:07:36

## 最高总纲
原始平面图 → 消防 CAD 自动画图 → 自动统计工程量 → 标准工程量清单 → 宏业自动报价 → 人工复核 → 最终报价成果。

## 当前宏业项目定位
后端报价引擎，只负责标准工程量清单到报价成果，不处理 CAD 绘图逻辑。

## 本轮状态
真实 DB3 关键词探针 + 匹配规则增强 V1 已完成。

## 本轮结果
- DB3 关键词探针是否成功：是。
- 当前输入记录数：2。
- 当前 2 条输入是否找到候选：否。
- 每条候选数量：0, 0。
- 匹配规则是否增强：是。
- 本地探针结果：output/hongye_real_db3_keyword_probe_v1.json（不提交）。
- 本地人工复核 Excel：output/宏业定额匹配人工复核_real_db3_probe_v1.xlsx（不提交）。
- 真实价格字段泄露风险：未发现。

## 关键判断
当前标准化输入是测试模板“测试清单项”，不是有效工程量清单，所以没有命中真实 DB3 候选。DB3 查询链路和安全边界已验证，下一轮应使用真实标准工程量清单样例。

## 请求 GPT 判断
请 GPT 判断下一步：
1. 是否让唐老板提供一份真实标准工程量清单中间结构样例。
2. 是否继续使用 V3 DB3 只读查询接口跑真实样例。
3. 是否根据人工复核结果继续优化关键词召回。
4. 是否进入“真实定额匹配接口 V4：字段映射 + 复核闭环”。

## 最高总纲偏离检查
- 是否服务于最终生产线：是。
- 是否保持宏业项目定位为后端报价引擎：是。
- 是否混入 CAD 绘图逻辑：否。
- 是否把 CAD 项目说成单纯读图算量：否。
- 是否继续推进标准工程量清单到报价成果链路：是。

## 安全边界
- 不提交真实 DB3 路径
- 不提交 DB3
- 不提交 output XLSX/JSON/CSV
- 不提交真实价格数据
- 不提交原始 XLSX
- 不提交 token/key/密码
- 不做 CAD/LISP/DWG/DXF
